<Window x:Class="CustomRoutedCommand.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomRoutedCommand"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <!--<Window.DataContext>
        <local:Window1VM x:Name="Wmw1" ></local:Window1VM>
    </Window.DataContext>-->
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="4*"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0">

            <Button  x:Name="button0" Content="Button0" Click="button0_Click"/>

            <Label x:Name="lab1" Content="{Binding Propstring, Mode=OneWay}"/>

            <Label x:Name="lab2" Content="{Binding OUser.Name , Mode=OneWay}"/>
            <Label x:Name="lab3" Content="{Binding OUser.LastName , Mode=OneWay}"/>

        </StackPanel>
    </Grid>
</Window>
